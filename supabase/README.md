# Supabase Backend

This directory is reserved for:

- database migrations
- SQL seed data for demo zones and alerts
- storage bucket setup for uploaded photos
- edge functions for `nearby-alerts` and `park-ranger-assistant`

Keep AI calls server-side so API keys never ship in the mobile app.
